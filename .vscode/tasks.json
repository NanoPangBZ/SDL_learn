{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build SDL Hello (Debug)",
            "type": "shell",
            "command": "${workspaceFolder}\\script\\build_debug.bat",
            "options": {
                "cwd": "${workspaceFolder}",
                "shell": {
                    "executable": "cmd.exe",
                    "args": [
                        "/d",
                        "/c"
                    ]
                }
            },
            "problemMatcher": "$gcc",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
